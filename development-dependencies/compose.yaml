# Use postgres/example user/password credentials

services:

  db:
    image: postgres:18.3
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    # ports:
    #   - 5432:5432

  # adminer:
  #   image: adminer:5
  #   restart: always
  #   ports:
  #     - 8080:8080
